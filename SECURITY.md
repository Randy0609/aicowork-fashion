# Security

## 报告问题

请不要在公开 Issue 中粘贴 API Key、客户资料、订单明细或其他敏感信息。

请进入本仓库的 **Security → Report a vulnerability**，通过 GitHub 私密漏洞报告提交问题。若该入口暂时不可用，请只在公开 Issue 中说明“需要安全联系方式”，不要附带漏洞细节或敏感数据。

当前维护范围为最新发布的 `0.1.x` 预发布版本。项目仍处于 Alpha 阶段，安全修复会随版本说明公开记录，但不承诺固定响应时限。

## 当前安全边界

- 模型密钥只从后端环境变量读取；
- API 不提供读取密钥的接口；
- 默认只监听容器或本机配置的端口；
- 工作流处理器是本地可信代码，不提供在线上传并执行 Python 代码的能力；
- 模拟数据不代表真实经营结果。
